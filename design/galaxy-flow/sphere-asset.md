# Approved sphere production asset

Generated with the built-in image generation tool, using `f1-start-and-voice-v2.png` as the approved reference. Original transparent PNG copied without raster editing to `web/images/thread-sphere.png` and `android/app/src/main/res/drawable-nodpi/thread_sphere.png`. Both implementations apply subtle audio-level motion at runtime and respect reduced-motion settings. This is one visual asset, not a flattened app screen.

Generation prompt:

> Use case: background-extraction / stylized-concept. Create a production bitmap asset for THREAD's approved Samsung-inspired native Android voice interface. Reference image shows the approved blue acoustic sphere on phone screens. Generate ONLY one isolated sphere matching that approved sphere, no phone, no text, no icons, no UI. Perfect circle centered on a square canvas, sphere diameter 78% of canvas width, generous transparent margins. Genuine transparent background/alpha. Premium luminous midnight-blue glassy sphere with thin precise cobalt and icy-blue rim, its interior mostly transparent dark navy, two or three fluid translucent blue acoustic membranes folding gently across the center like the reference, smooth dimensional light, sophisticated optical depth. Controlled atmospheric glow close to the perimeter. It must look like the reference's crafted illuminated object, not a flat icon, wireframe, dense mathematical sine plot, lava lamp, confetti or neon cyberpunk. No words or extra elements. High resolution clean edges. This is the actual hero visual asset and will receive subtle amplitude-driven native motion; keep a composed symmetric resting pose.
